# Base URL of the admin backend API (see munay/backend). Used by src/admin/lib/api.ts.
# Not yet consumed by any page — the admin UI still runs on static mock data.
VITE_API_URL=http://localhost:3001
